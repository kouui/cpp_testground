{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "type": "shell",
            "command": "g++ -std=c++14 -g ${file} -o Demo",
            "group":
            {
                "kind": "build",
                "isDefault": true,
            },
            "problemMatcher": "$gcc",
        }
    ]
}